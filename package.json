{
  "name": "rik",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --cwd app dev",
    "worker": "bun run --env-file=.env app/worker/index.ts",
    "infra": "docker compose up redis minio -d",
    "infra:all": "docker compose up -d",
    "infra:stop": "docker compose down",
    "db:generate": "bun run --cwd app db:generate",
    "db:migrate": "bun run --cwd app db:migrate",
    "db:studio": "bun run --cwd app db:studio",
    "rik": "bun run packages/cli/src/index.ts"
  }
}
